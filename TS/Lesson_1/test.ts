{
  const teachers: string[] = ['Hoang', 'Dat', 'Tu', 'Ngoc', 'Hoa']
}

{
  const myString: string = 'Hom nay troi dep qua!'
  console.log(myString.toLowerCase())
  console.log(myString.toLocaleUpperCase())
}

type user = {
  name: string
  age: number
  address: string
  jobs: string[]
  company: string
  isMarry: boolean
  gender: string
  hobbies: string[]
}

{
  const myInfor: user = {
    name: 'Nguyen Thi Gam',
    age: 19,
    address: 'Ha Noi',
    jobs: ['students', 'sale'],
    company: 'Winmart',
    isMarry: false,
    gender: 'Nu',
    hobbies: ['sing', 'play mussician', 'dance', 'move', 'reading']
  }
}
